[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22.17.0"
  # Disable secrets scanning for public API URLs
  SECRETS_SCAN_ENABLED = "false"
